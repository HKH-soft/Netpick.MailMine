package ir.netpick.mailmine.common.enums;

public enum ProxyProtocol {
    // Standard proxies
    HTTP,
    HTTPS,
    SOCKS4,
    SOCKS5,

    // V2Ray protocols
    VLESS,
    VMESS,
    SHADOWSOCKS,
    TROJAN
}
