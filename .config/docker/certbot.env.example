# Domain configuration
CERTBOT_DOMAIN=your-doamin.com
CERTBOT_DOMAIN_WWW=www.your-domain.com

# Email for important notifications (certificate expiration, etc.)
CERTBOT_EMAIL=youremail@example.ir

# RSA key size (2048 or 4096)
RSA_KEY_SIZE=4096

# DNS propagation wait time in seconds (increase if DNS updates are slow)
DNS_PROPAGATION_SECONDS=60

# Use Let's Encrypt staging environment for testing (0=production, 1=staging)
# Set to 1 to avoid rate limits during testing
STAGING=0
